import { motion } from 'framer-motion';\n\nconst StatsCard = ({ title, value, icon: Icon, color = \"primary\", trend, delay = 0 }) => {\n  const colorClasses = {\n    primary: \"bg-primary-500 text-white\",\n    success: \"bg-success-500 text-white\",\n    warning: \"bg-warning-500 text-white\",\n    danger: \"bg-danger-500 text-white\"\n  };\n\n  return (\n    <motion.div\n      initial={{ y: 20, opacity: 0 }}\n      animate={{ y: 0, opacity: 1 }}\n      transition={{ delay }}\n      whileHover={{ y: -5, scale: 1.02 }}\n      className=\"card p-6 relative overflow-hidden\"\n    >\n      {/* Icône de fond */}\n      <div className=\"absolute top-4 right-4 opacity-10\">\n        <Icon size={60} className=\"text-gray-400\" />\n      </div>\n\n      {/* Contenu */}\n      <div className=\"relative z-10\">\n        <div className=\"flex items-center gap-3 mb-4\">\n          <div className={`w-12 h-12 rounded-lg flex items-center justify-center ${colorClasses[color]}`}>\n            <Icon size={24} />\n          </div>\n          <div>\n            <p className=\"text-sm text-gray-600 font-medium\">{title}</p>\n            {trend && (\n              <p className={`text-xs ${trend > 0 ? 'text-success-600' : 'text-danger-600'}`}>\n                {trend > 0 ? '+' : ''}{trend}% ce mois\n              </p>\n            )}\n          </div>\n        </div>\n        \n        <motion.p \n          initial={{ scale: 0.5 }}\n          animate={{ scale: 1 }}\n          transition={{ delay: delay + 0.2, type: \"spring\", stiffness: 200 }}\n          className=\"text-3xl font-bold text-gray-900\"\n        >\n          {value}\n        </motion.p>\n      </div>\n\n      {/* Effet de brillance au survol */}\n      <motion.div\n        className=\"absolute inset-0 bg-gradient-to-r from-transparent via-white to-transparent opacity-0\"\n        whileHover={{ opacity: 0.1, x: ['-100%', '100%'] }}\n        transition={{ duration: 0.6 }}\n      />\n    </motion.div>\n  );\n};\n\nexport default StatsCard;