const express = require('express');
const router = express.Router();

// Placeholder pour les routes de frais (à développer dans Sprint 2)
router.get('/', (req, res) => {
  res.json({ message: 'Routes frais - À implémenter' });
});

module.exports = router;
